# Copy to .env and store secrets securely; do not commit real keys
# WARNING: Hardcoded API keys are a security risk (see CWE-798). Use a secrets manager and rotate keys regularly.

# API keys for optional providers
GEMINI_API_KEY=your_gemini_key
OPENAI_API_KEY=your_openai_key
OPENROUTER_API_KEY=your_openrouter_key

# Optional: set your public app URL for server-side rendering
VITE_APP_URL=https://your-domain.example

# Persistent memory (Cipher)
VITE_USE_CIPHER_MEMORY=false
VITE_CIPHER_SERVER_URL=http://localhost:3000
# Optional: enforce CSP headers from the memory server
VITE_ENFORCE_CIPHER_CSP=false
VITE_CIPHER_CIRCUIT_BREAKER_THRESHOLD=5
VITE_CIPHER_CIRCUIT_BREAKER_RESET_MS=30000
